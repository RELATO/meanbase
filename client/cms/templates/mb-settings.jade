
h2 Themes
#mb-site-theme
    .d-4.t-6(ng-repeat="theme in themes").mb-theme
        h4 {{theme.title}} 
        //- img(src="http://placehold.it/300x200")
        img(src="/{{theme.preview}}").img-responsive.img-thumbnail
        .modal.fade(id="theme-{{$index}}" tabindex='-1', role='dialog', aria-labelledby='theme-{{$index}}Label', aria-hidden='true')
            .modal-dialog
                .modal-content
                    .modal-header
                        button.close(type='button', data-dismiss='modal')
                            span(aria-hidden='true') ×
                            span.sr-only Close
                        h4(id="theme-{{$index}}").modal-title {{theme.title}}
                    .modal-body
                        .mb-theme-controls.a-left
                            .form-group
                                label Author
                                input(type="text" value="{{theme.author}}" disabled).form-control
                            .form-group
                                label Email
                                input(type="text" value="{{theme.email}}" disabled).form-control
                            .form-group
                                label Website
                                input(type="text" value="{{theme.website}}" disabled).form-control
                            .form-group
                                label Description
                                textarea(disabled).form-control {{theme.description}}
                    .modal-footer
                        button.btn.btn-warning(type='button' ng-click="updateTheme(theme)" data-dismiss='modal') Update
                        button.btn.btn-primary(type='button', data-dismiss='modal') Close
                        
        .mb-theme-buttons
            button.btn.btn-warning(data-toggle='modal' data-target='#theme-{{$index}}') Info
            button.btn.btn-danger(ng-click="deleteTheme(theme, $index)") Delete
            button.btn.btn-success(ng-disabled="theme.active" ng-click="activateTheme(theme, $index)") Activate
            span(ng-if="theme.active").mb-active-theme Active
h3 Social Sharing Setup
button.mb-button.mb-connect-social Connect
