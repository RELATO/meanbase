#editPageModal.modal.fade(tabindex='-1', role='dialog', aria-labelledby='editPageMeta', aria-hidden='true')
	.modal-dialog
		.modal-content
			.modal-header
				button.close(type='button', data-dismiss='modal')
					span(aria-hidden='true') ×
					span.sr-only Close
				h4#editPageMeta.modal-title Page Settings
			.modal-body
				.form-group
					label Google Search Title
					input.form-control(type='text' ng-model="page.tabTitle")
				.form-group
					label URL address
					input.form-control(type='text' ng-model="page.url")
				.form-group
					label Facebook and Google Page Description
					textarea.form-control(rows="5" ng-model="page.description")
				.row
					.col-sm-6
						.form-group
							label Who can see?
							select(ng-model="page.visibility").form-control
								option(value='everybody') Everybody
								option(ng-repeat="role in roles" value="{{role.role}}") {{role.role}}
							p.h5 and up
					.col-sm-6
						.form-group
							label Who can edit?
							select(ng-model="page.editability").form-control
								option(value='everybody') Everybody
								option(ng-repeat="role in roles" value="{{role.role}}") {{role.role}}
							p.h5 and up
				.row
					.col-sm-6
						p.h5 Created
						p - {{page.created | date: 'MMM d, y h:mm a'}}
					.col-sm-6
						p.h5 Updated
						p - {{page.updated | date: 'MMM d, y h:mm a'}}
			.modal-footer
				.col-sm-9
					p.h6.text-left (The headbar save and discard buttons will store changes)
				.col-sm-3
					button.btn.btn-default(type='button', data-dismiss='modal') Close